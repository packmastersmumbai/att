# Dependencies
node_modules/

# Playwright artifacts
.playwright-cli/
test-results/
test-report/
tests/

# Screenshots / debug images
*.png

# E2E suites ARE source: 226 assertions, and tests/helpers/gas-mock.js was
# already tracked while every suite reading it was not — the mock was
# versioned, the tests were not. package.json comes with them or a fresh
# clone cannot install Playwright to run any of it.
playwright.config.js
package-lock.json
run_params.json

# Design/product docs (not source)
DESIGN.json
DESIGN.md
PRODUCT.md

# Google Apps Script deploy config
.clasp.json
